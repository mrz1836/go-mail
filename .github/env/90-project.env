# ================================================================================================
# 📦 PROJECT CONFIGURATION (90-project.env)
# ================================================================================================
#
# Purpose: Project-specific overrides for go-mail
# Load Order: 90 (loaded near-last, overrides all default/tool configurations)
#
# This file contains project-specific settings that override defaults from earlier
# configuration files (00-core.env, 10-*.env, 20-*.env).
#
# Only include variables that DIFFER from the defaults defined in lower-numbered files.
#
# ================================================================================================

# ================================================================================================
# 📊 COVERAGE CONFIGURATION OVERRIDES
# ================================================================================================

# Coverage Provider Selection (overrides 10-coverage.env default of "internal")
GO_COVERAGE_PROVIDER=codecov

# Codecov Configuration (overrides 10-coverage.env default of false)
CODECOV_TOKEN_REQUIRED=true

# Coverage Exclusions (matches codecov.yml ignore paths + standard Go exclusions)
GO_COVERAGE_EXCLUDE_PATHS=test/,vendor/,testdata/,.github/,.mage-cache/,.vscode/,bin/,example/,examples/,mocks/,testing/
